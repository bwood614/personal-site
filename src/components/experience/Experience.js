import React from "react";

const Experience = () => {
  const style = {
    title: {
      color: "blue",
    },
  };

  return <h1 style={style.title}>Experience</h1>;
};

export default Experience;
